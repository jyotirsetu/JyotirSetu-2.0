---
export const prerender = false;
---
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Settings – Admin</title>
<link rel="stylesheet" href="/admin.css?v=20251108" />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <script src="/js/admin-notify.js" defer></script>
    <style>
      .settings-section { background: linear-gradient(180deg, var(--panel), var(--panel-2)); border: 1px solid var(--border); border-radius: 14px; padding: 24px; margin-bottom: 20px; }
      .settings-section h3 { margin: 0 0 16px; font-size: 18px; font-weight: 700; }
      .form-row { display: grid; grid-template-columns: 200px 1fr; gap: 16px; margin-bottom: 16px; align-items: center; }
      .form-row label { font-weight: 500; color: var(--text); }
      .form-row .input, .form-row .select { flex: 1; }
      .toggle-switch { position: relative; display: inline-block; width: 50px; height: 24px; }
      .toggle-switch input { opacity: 0; width: 0; height: 0; }
      .toggle-slider { position: absolute; cursor: pointer; top: 0; left: 0; right: 0; bottom: 0; background-color: #374151; transition: .4s; border-radius: 24px; }
      .toggle-slider:before { position: absolute; content: ""; height: 18px; width: 18px; left: 3px; bottom: 3px; background-color: white; transition: .4s; border-radius: 50%; }
      input:checked + .toggle-slider { background-color: var(--brand); }
      input:checked + .toggle-slider:before { transform: translateX(26px); }
      .info-text { font-size: 12px; color: var(--muted); margin-top: 4px; }
    </style>
  </head>
  <body>
    <button class="sidebar-toggle" aria-label="Open Admin Menu" id="sidebarToggle">☰</button>
    <div class="sidebar-backdrop" id="sidebarBackdrop"></div>
    <div class="layout">
      <aside class="sidebar" id="adminSidebar">
        <div class="logo"><span class="brand-dot"></span> <span>JyotirSetu Admin</span></div>
        <nav class="nav">
          <a href="/admin/dashboard">Dashboard</a>
          <a href="/admin/appointments">Appointments</a>
          <a href="/admin/contacts">Contacts</a>
          <a href="/admin/calendar">Calendar</a>
          <a href="/admin/activity-log">Activity Log</a>
          <a href="/admin/tools/follow-hub/insights">Follow Hub – Insights</a>
          <a href="/admin/templates/email">Email Templates</a>
          <a href="/admin/templates/whatsapp">WhatsApp Templates</a>
          <a class="active" href="/admin/settings">Settings</a>
        </nav>
      </aside>
      <section class="content">
        <div class="topbar">
          <strong>Settings</strong>
          <div style="display:flex; gap:8px; align-items:center">
            <a href="/api/admin/logout" class="pill">Logout</a>
          </div>
        </div>
        <div style="padding: 20px;">
          <div class="settings-section">
            <h3>🔐 Account Settings</h3>
            <div class="form-row">
              <label>Admin Username</label>
              <div>
                <input class="input" id="username" type="text" placeholder="Current username" disabled />
                <div class="info-text">Change via ADMIN_USERNAME environment variable</div>
              </div>
            </div>
            <div class="form-row">
              <label>Admin Password</label>
              <div>
                <input class="input" id="password" type="password" placeholder="Enter new password" />
                <div class="info-text">Change via ADMIN_PASSWORD environment variable</div>
              </div>
            </div>
            <div class="form-row">
              <label>Session Duration</label>
              <select class="select" id="sessionDuration">
                <option value="7">7 days</option>
                <option value="30" selected>30 days</option>
                <option value="90">90 days</option>
              </select>
            </div>
          </div>

          <div class="settings-section">
            <h3>🔔 Notifications</h3>
            <div class="form-row">
              <label>Browser Notifications</label>
              <label class="toggle-switch">
                <input type="checkbox" id="browserNotifications" />
                <span class="toggle-slider"></span>
              </label>
            </div>
            <div class="form-row">
              <label>Email Alerts</label>
              <label class="toggle-switch">
                <input type="checkbox" id="emailAlerts" checked />
                <span class="toggle-slider"></span>
              </label>
            </div>
            <div class="form-row">
              <label>Notify on New Appointment</label>
              <label class="toggle-switch">
                <input type="checkbox" id="notifyNewAppointment" checked />
                <span class="toggle-slider"></span>
              </label>
            </div>
            <div class="form-row">
              <label>Notify on New Contact</label>
              <label class="toggle-switch">
                <input type="checkbox" id="notifyNewContact" checked />
                <span class="toggle-slider"></span>
              </label>
            </div>
          </div>

          <div class="settings-section">
            <h3>📧 Email Configuration</h3>
            <div class="form-row">
              <label>Email Service</label>
              <div>
                <span class="pill" style="background:#22c55e">SMTP (Active)</span>
                <div class="info-text" style="margin-top: 8px;">SMTP configuration is managed via environment variables (SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASS)</div>
              </div>
            </div>
            <div class="form-row">
              <label>Test Email</label>
              <div style="display:flex;gap:8px;align-items:center">
                <input class="input" id="testEmail" type="email" placeholder="Enter email to test" style="flex:1;max-width:300px" />
                <button class="btn-sm" id="testEmailBtn" style="background:#667eea;color:white;border:none;padding:8px 16px;border-radius:6px;cursor:pointer">Send Test</button>
              </div>
            </div>
          </div>

          <div class="settings-section">
            <h3>🗄️ Database</h3>
            <div class="form-row">
              <label>Database Status</label>
              <div>
                <span class="pill" id="dbStatus">Checking...</span>
              </div>
            </div>
            <div class="form-row">
              <label>Backup Database</label>
              <button class="btn-sm" id="backupBtn" style="background:#667eea;color:white;border:none;padding:8px 16px;border-radius:6px;cursor:pointer">Download Backup</button>
            </div>
          </div>

          <div class="settings-section">
            <h3>🎨 Appearance</h3>
            <div class="form-row">
              <label>Theme</label>
              <select class="select" id="theme">
                <option value="dark">Dark (Default)</option>
                <option value="light">Light</option>
                <option value="auto">Auto (System)</option>
              </select>
            </div>
            <div class="form-row">
              <label>Items Per Page</label>
              <select class="select" id="itemsPerPage">
                <option value="10">10</option>
                <option value="20" selected>20</option>
                <option value="50">50</option>
                <option value="100">100</option>
              </select>
            </div>
            <div class="form-row">
              <label>Compact Mode</label>
              <label class="toggle-switch">
                <input type="checkbox" id="compactMode" />
                <span class="toggle-slider"></span>
              </label>
            </div>
          </div>

          <div class="settings-section">
            <h3>⚙️ General Settings</h3>
            <div class="form-row">
              <label>Auto-refresh Interval</label>
              <select class="select" id="autoRefresh">
                <option value="0">Disabled</option>
                <option value="30" selected>30 seconds</option>
                <option value="60">1 minute</option>
                <option value="300">5 minutes</option>
              </select>
            </div>
            <div class="form-row">
              <label>Show Email Preview</label>
              <label class="toggle-switch">
                <input type="checkbox" id="showEmailPreview" checked />
                <span class="toggle-slider"></span>
              </label>
            </div>
            <div class="form-row">
              <label>Confirm Before Delete</label>
              <label class="toggle-switch">
                <input type="checkbox" id="confirmDelete" checked />
                <span class="toggle-slider"></span>
              </label>
            </div>
            <div class="form-row">
              <label>Show Timestamps</label>
              <label class="toggle-switch">
                <input type="checkbox" id="showTimestamps" checked />
                <span class="toggle-slider"></span>
              </label>
            </div>
          </div>

          <div class="settings-section">
            <h3>📊 Dashboard Settings</h3>
            <div class="form-row">
              <label>Show Last 24h Stats</label>
              <label class="toggle-switch">
                <input type="checkbox" id="show24hStats" checked />
                <span class="toggle-slider"></span>
              </label>
            </div>
            <div class="form-row">
              <label>Show Pending Actions</label>
              <label class="toggle-switch">
                <input type="checkbox" id="showPendingActions" checked />
                <span class="toggle-slider"></span>
              </label>
            </div>
            <div class="form-row">
              <label>Default View</label>
              <select class="select" id="defaultView">
                <option value="dashboard">Dashboard</option>
                <option value="appointments">Appointments</option>
                <option value="contacts">Contacts</option>
                <option value="calendar">Calendar</option>
              </select>
            </div>
          </div>

          <div style="margin-top: 24px; display: flex; gap: 12px; justify-content: flex-end;">
            <button class="btn-primary" id="saveBtn" style="width:100%;padding:12px;font-size:16px;margin-top:20px">Save Settings</button>
          </div>
        </div>
      </section>
    </div>

    <script>
      /**
       * @typedef {object} Settings
       * @property {string} theme
       * @property {number} itemsPerPage
       * @property {boolean} compactMode
       * @property {number} sessionDuration
       * @property {boolean} browserNotifications
       * @property {boolean} emailAlerts
       * @property {boolean} notifyNewAppointment
       * @property {boolean} notifyNewContact
       * @property {number} autoRefresh
       * @property {boolean} showEmailPreview
       * @property {boolean} confirmDelete
       * @property {boolean} showTimestamps
       */

      /** @type {Settings} */
      const DEFAULT_SETTINGS = {
        theme: 'dark',
        itemsPerPage: 20,
        compactMode: false,
        sessionDuration: 30,
        browserNotifications: false,
        emailAlerts: true,
        notifyNewAppointment: true,
        notifyNewContact: true,
        autoRefresh: 30,
        showEmailPreview: true,
        confirmDelete: true,
        showTimestamps: true,
      };

      /** @returns {Settings} */
      function getSettings() {
        try {
          const stored = localStorage.getItem('adminSettings');
          if (stored) return { ...DEFAULT_SETTINGS, ...JSON.parse(stored) };
        } catch (e) {
          console.error('Failed to parse settings', e);
        }
        return DEFAULT_SETTINGS;
      }

      /** @param {Partial<Settings>} newSettings */
      function saveSettings(newSettings) {
        const current = getSettings();
        const updated = { ...current, ...newSettings };
        localStorage.setItem('adminSettings', JSON.stringify(updated));
        applySettings(updated);
      }

      /** @param {Settings} settings */
      function applySettings(settings) {
        // Theme
        document.documentElement.dataset.theme = settings.theme;
        if (settings.theme === 'auto') {
          const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
          document.documentElement.dataset.theme = prefersDark ? 'dark' : 'light';
        }

        // Other settings
        setInputValue('theme', settings.theme);
        setInputValue('itemsPerPage', settings.itemsPerPage);
        setInputValue('compactMode', settings.compactMode);
        setInputValue('sessionDuration', settings.sessionDuration);
        setInputValue('browserNotifications', settings.browserNotifications);
        setInputValue('emailAlerts', settings.emailAlerts);
        setInputValue('notifyNewAppointment', settings.notifyNewAppointment);
        setInputValue('notifyNewContact', settings.notifyNewContact);
        setInputValue('autoRefresh', settings.autoRefresh);
        setInputValue('showEmailPreview', settings.showEmailPreview);
        setInputValue('confirmDelete', settings.confirmDelete);
        setInputValue('showTimestamps', settings.showTimestamps);
      }

      /**
       * @param {string} id
       * @param {string | number | boolean} value
       */
      function setInputValue(id, value) {
        const el = document.getElementById(id) as HTMLInputElement | HTMLSelectElement | null;
        if (!el) return;
        if (el.type === 'checkbox') {
          el.checked = !!value;
        } else {
          el.value = String(value);
        }
      }

      /**
       * @param {string} id
       * @returns {string | number | boolean}
       */
      function getInputValue(id) {
        const el = document.getElementById(id) as HTMLInputElement | HTMLSelectElement | null;
        if (!el) return null;
        if (el.type === 'checkbox') return el.checked;
        if (el.type === 'number' || el.tagName === 'SELECT') return Number(el.value);
        return el.value;
      }

      document.addEventListener('DOMContentLoaded', () => {
        const settings = getSettings();
        applySettings(settings);

        const saveBtn = document.getElementById('saveBtn') as HTMLButtonElement | null;
        saveBtn?.addEventListener('click', () => {
          /** @type {Partial<Settings>} */
          const newSettings = {
            theme: getInputValue('theme'),
            itemsPerPage: getInputValue('itemsPerPage'),
            compactMode: getInputValue('compactMode'),
            sessionDuration: getInputValue('sessionDuration'),
            browserNotifications: getInputValue('browserNotifications'),
            emailAlerts: getInputValue('emailAlerts'),
            notifyNewAppointment: getInputValue('notifyNewAppointment'),
            notifyNewContact: getInputValue('notifyNewContact'),
            autoRefresh: getInputValue('autoRefresh'),
            showEmailPreview: getInputValue('showEmailPreview'),
            confirmDelete: getInputValue('confirmDelete'),
            showTimestamps: getInputValue('showTimestamps'),
          };
          saveSettings(newSettings);
          window.AdminNotify?.success('Settings saved');
        });

        // Check DB status
        fetch('/api/db-health').then(res => res.json()).then(data => {
          const statusEl = document.getElementById('dbStatus') as HTMLElement | null;
          if (!statusEl) return;
          if (data.ok) {
            statusEl.textContent = 'Connected';
            statusEl.style.background = '#22c55e';
          } else {
            statusEl.textContent = 'Error';
            statusEl.style.background = '#ef4444';
          }
        });

        // Test email
        const testEmailBtn = document.getElementById('testEmailBtn') as HTMLButtonElement | null;
        testEmailBtn?.addEventListener('click', async () => {
          const email = getInputValue('testEmail');
          if (!email || !/\S+@\S+\.\S+/.test(String(email))) {
            window.AdminNotify?.error('Invalid email', 'Please enter a valid email address');
            return;
          }
          const btn = document.getElementById('testEmailBtn') as HTMLButtonElement | null;
          if (btn) { btn.textContent = 'Sending...'; btn.disabled = true; }
          try {
            const res = await fetch('/api/test-email', {
              method: 'POST',
              headers: { 'Content-Type': 'application/json' },
              body: JSON.stringify({ email }),
            });
            const result = await res.json();
            if (result.ok) window.AdminNotify?.success('Test email sent');
            else window.AdminNotify?.error('Email error', result.error || 'Failed to send');
          } catch (e) {
            window.AdminNotify?.error('Error occurred', e.message);
          }
          if (btn) { btn.textContent = 'Send Test'; btn.disabled = false; }
        });

        // Backup DB
        const backupBtn = document.getElementById('backupBtn') as HTMLButtonElement | null;
        backupBtn?.addEventListener('click', () => {
          window.location.href = '/api/admin/export';
        });
      });
    </script>
  </body>
</html>

